{
  "version": 2,
  "buildCommand": "cd server && npx prisma migrate deploy && cd .. && npm run build -- --mode production",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd server && npm install && cd .. && npx prisma generate --schema=./server/prisma/schema.prisma",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_SCHEMA_PATH": "server/prisma/schema.prisma"
  }
}
